[build-system] 
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fetch-weather-cli"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "requests"
]

[project.scripts]
weather = "main:main"

[tool.hatch.build.targets.wheel]
include = ["*.py"]